name: Quality gates

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.19.0
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm test
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
      - run: npm run test:e2e -- --project=chromium
      - name: Verify supported Node range is declared
        run: node -e "if(require('./package.json').engines.node!=='^20.19.0 || >=22.12.0')process.exit(1)"
